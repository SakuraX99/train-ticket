spring:
  application:
    name: ts-price-service
  datasource:
    url: jdbc:mysql://10.176.122.80:31113/ts-price-mysql?useSSL=false
    #    url: jdbc:mysql://ts-price-mysql:3306/ts-price-mysql
    username: root
    password: root
    driver-class-name: com.mysql.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update


# HTTP Server
server:
  port: 16579  # HTTP (Tomcat) port

swagger:
  controllerPackage: price.controller